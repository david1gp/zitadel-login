# Safe local-development placeholders only. Never put credentials in this file.
ZITADEL_ORIGIN=https://your-zitadel.example
ZITADEL_ORGANIZATION_ID=replace-with-organization-id
ZITADEL_ALLOWED_CLIENT_IDS=replace-with-client-id,replace-with-another-client-id
LOGIN_V2_FALLBACK_URL=https://your-zitadel.example/ui/v2/login
PAGES_ORIGIN=https://your-pages.example
SESSION_LIFETIME_SECONDS=900
ZITADEL_CUSTOM_LOGIN_ENABLED=false
TERMS_OF_SERVICE_URL=https://your-site.example/terms
PRIVACY_POLICY_URL=https://your-site.example/privacy

# Set these only through encrypted Worker secrets in deployed environments.
ZITADEL_LOGIN_CLIENT_PAT=
# 32 random bytes encoded as unpadded base64url.
FLOW_COOKIE_KEY=
ZITADEL_PASSWORD_RESET_V2_ENABLED=false
# Optional recent-account persistence. Keep these keys separate from FLOW_COOKIE_KEY.
RECENT_ACCOUNT_COOKIE_KEY=
RECENT_ACCOUNT_COOKIE_PREVIOUS_KEY=
ZITADEL_RECENT_ACCOUNT_V2_ENABLED=false

# Native organization OTP Email policy/enrollment command. Dry-run is the safe default.
ZITADEL_ADMIN_PAT=
ZITADEL_ORGANIZATION_NAME=replace-with-exact-organization-name
ZITADEL_OTP_CONFIRM_EMAIL=replace-with-exact-canary-email
ZITADEL_OTP_MODE=dry-run

# Dedicated public OIDC + PKCE test-client provisioning. Dry-run is the safe default.
ZITADEL_E2E_MODE=dry-run

import type { JSX } from "solid-js"

export const stylesBgSlash = {
  "background-image":
    "repeating-linear-gradient(135deg, color-mix(in srgb, var(--brand-font) 41%, transparent) 0 1px, #0000 1px 12px)",
} as const satisfies JSX.CSSProperties

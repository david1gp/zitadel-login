import { classArr } from "../classArr"

export const classesPageShell = classArr(
  "page-shell h-max min-h-dvh w-full grid place-items-[start_center] p-0 text-foreground bg-background",
  "sm:place-items-center sm:px-5 sm:py-8",
)

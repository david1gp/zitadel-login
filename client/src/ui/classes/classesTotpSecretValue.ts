import { classArr } from "../classArr"

export const classesTotpSecretValue = classArr(
  "flex flex-wrap gap-2.5 border border-foreground-18 rounded-[9px] p-3 bg-foreground-4",
  "font-mono text-base tracking-[0.14em] wrap-anywhere",
)

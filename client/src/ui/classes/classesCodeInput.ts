import { classArr } from "../classArr"

import { classesInput } from "./classesInput"

export const classesCodeInput = classArr(
  classesInput,
  "h-[62px] text-center text-[26px] font-extrabold tracking-[0.28em] tabular-nums",
)

import { classArr } from "../classArr"

export const classesThemeToggle = classArr(
  "flex min-w-0 min-h-[38px] m-0 border border-foreground-25 rounded-full p-[3px]",
  "text-foreground bg-transparent disabled:opacity-65",
)

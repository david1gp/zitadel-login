import { classArr } from "../classArr"

export const classesLanguageSwitcherSummary = classArr(
  "inline-flex items-center gap-1.5 min-h-[38px] cursor-pointer list-none",
  "border border-foreground-25 rounded-full py-[3px] px-3",
  "text-foreground text-[13px] font-[750] bg-transparent",
  "hover:bg-primary-10 [&::-webkit-details-marker]:hidden",
)

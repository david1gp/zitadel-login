import { classArr } from "../classArr"

export const classesRecentAccountButton = classArr(
  "flex items-center gap-3.5 w-full min-h-16 border border-foreground-25 rounded-[10px]",
  "px-3.5 py-2.5 text-foreground bg-transparent text-left",
  "hover:enabled:bg-primary-10",
)

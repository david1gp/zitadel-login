import { classArr } from "../classArr"

export const classesLanguageSwitcherMenu = classArr(
  "absolute end-0 z-10 mt-1 flex flex-col min-w-[14ch] max-h-[60vh] overflow-auto",
  "rounded-lg border border-foreground-25 bg-background p-1 shadow-lg",
)

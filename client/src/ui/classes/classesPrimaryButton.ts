import { classArr } from "../classArr"

export const classesPrimaryButton = classArr(
  "w-full min-h-[50px] mt-[18px] border border-primary rounded-[9px] px-4",
  "text-primary-foreground bg-primary font-extrabold",
  "forced-colors:border-2 forced-colors:border-[ButtonText]",
)

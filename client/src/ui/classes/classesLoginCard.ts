import { classArr } from "../classArr"

export const classesLoginCard = classArr(
  "w-[min(100%,470px)] min-h-dvh overflow-hidden border-0 rounded-none",
  "text-foreground bg-card shadow-none",
  "sm:min-h-0 sm:border sm:border-foreground-18 sm:rounded-[18px] sm:shadow-login",
)

import { classArr } from "../classArr"

export const classesDemoGithubLink = classArr(
  "inline-flex items-center justify-center size-[30px] min-h-[30px] border-0 rounded-full p-0",
  "text-inherit bg-transparent no-underline",
  "hover:bg-primary-10",
)
